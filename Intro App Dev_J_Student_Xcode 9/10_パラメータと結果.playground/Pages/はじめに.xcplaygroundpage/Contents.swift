/*:
 ## 関数、ふたたび
 
 関数についての学習では、次のように関数を呼び出しました。
 ```
 (例): doSomething()
 ```
 少し違う方法で関数を呼び出したこともありますね。
 ```
 (例): print("Hello, world!")
 ```
 関数`print()`が機能するには、括弧内に値が入っている必要があります。その値を渡すことができないと、`print()`は何を出力すればよいかわからず機能しません。
 
 次は、関数に値を渡すことについて学習しましょう。
 
1／17ページ  |  [次は「具体的なものから一般的なものへ」です。](@next)
*/
